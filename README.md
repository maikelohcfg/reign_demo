#  Proyecto demo para Reign


1- Install cocoa pods
   $ sudo gem install cocoapods

2- Install dependencies (must be executed on project root folder)
  $ pod install
  
3- Open xcode project  from "demo.xcworkspace" file  
